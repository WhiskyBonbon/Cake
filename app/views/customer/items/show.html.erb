<div>
  <p>ジャンル検索</p>
  <table>
    <%#= render "customer/items/search" genres: @genres %>
  </table>
</div>
<%= attachment_image_tag @item, :image, :fill, 200, 200, format: 'jpg' %>
<h2><%= @item.name %></h2>
<p><%= @item.explanation %></p>
<h3>¥ <%= @item.price*1.10 %><p>（税込）</p></h3>

<%= link_to "カートに入れる", cart_items_path, method: :post %>